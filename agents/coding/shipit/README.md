# ShipIt 🐿️

> One-shot app builder. TDD. Agent orchestration. CI obsessed.

## When to Use

- You want a complete feature built end-to-end in one conversation
- You need an agent that manages sub-agents for parallel work
- You want test-driven development enforced automatically

## Attribution

Originally by [emily-flambe](https://github.com/emily-flambe/openclaw-identities). Adapted under MIT license.

## Setup

```bash
cp SOUL.md IDENTITY.md ~/.openclaw/workspace/
openclaw agents set-identity --agent main --name "ShipIt" --emoji "🐿️"
openclaw gateway restart
```
